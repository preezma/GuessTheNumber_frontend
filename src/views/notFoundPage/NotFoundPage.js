import { FlexContainer } from "../gamePage/StyledGamePage";

const NotFoundPage = () => {
  return (
    <FlexContainer>
      <h1>Oops, page is not found! :(</h1>
    </FlexContainer>
  );
};

export default NotFoundPage;
